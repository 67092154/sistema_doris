import React from 'react';
import { Card } from 'primereact/card';

const Bienvenida = () => {
    return (
        <Card title="Bienvenido a Administración Tropical" style={{ textAlign: 'center' }}>
            <p>Utilice el menú para navegar a través de las diferentes secciones.</p>
        </Card>
    );
};

export default Bienvenida;
